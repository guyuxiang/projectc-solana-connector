apiVersion: v1
kind: Service
metadata:
  name: projectc-solana-connector
  labels:
    app: projectc-solana-connector
spec:
  selector:
    app: projectc-solana-connector
  ports:
    - port: 8081
      targetPort: 8081
      protocol: TCP
      name: http
